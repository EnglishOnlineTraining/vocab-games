# Dependencies (generator scripts run under Node)
node_modules/
package-lock.json

# Environment / secrets — never commit
.env
.env.*

# Python
__pycache__/
*.pyc

# OS noise
.DS_Store
Thumbs.db

# Editor / tooling
.idea/
.vscode/
*.swp
*~

# Claude Code local settings (may contain secrets)
.claude/settings.local.json

# Logs and scratch output
*.log
/tmp/

# Audit script run-state (ephemeral, per-machine)
data/*-state.json
